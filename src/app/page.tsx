import React from 'react';
import SignUpLoginPage from './sign-up-login-screen/page';

// Entry point — renders Sign Up / Login as the root page
export default function RootPage() {
  return <SignUpLoginPage />;
}